# 【支払】業者承諾確認 詳細設計書

> **本書はドラフト（枠のみ）**。画面一覧の改訂（見積管理_処理フロー.xlsx）で追加された画面のため、
> ファイルのみ先行して作成している。仕様確定後に各節を埋めること。未確定箇所は `TBD` を残す。

> 共通の仕組み（ヘッダー / フィルタ / やり取り / iframe / テーマ / 送信・エラー方針）は
> [共通仕様_詳細設計.md](../quotations/00_共通仕様_詳細設計.md) を参照。本書は本画面固有の差分を記述する。

## 1. 概要

- **画面名**: 【支払】業者承諾確認【発注承諾済み→FELIX(担当者)】
- **区分**: 支払（FELIX が業者へ支払う側のフロー）
- **mode**: `TBD`
- **操作ロール**: TBD（担当者 / 建設部）
- **目的**: TBD — 業者が発注を承諾した案件を FELIX(担当者) が確認する。

業務フロー上の位置づけ:

```
発注承認 ──▶【業者承諾確認】──▶ 完了・納品
```

## 2. 機能説明

本画面の操作は、**画面上部の「絞り込み・操作バー」のボタン**と、**明細（各行）のボタン**の
2つのボタン群から行う。以下ではボタンごとに実行される処理を分けて記載する。

前提となる一覧表示: TBD（抽出条件は §4）。

### 2.1 絞り込み・操作バーのボタン（画面上部）

| ボタン / 操作 | 実行される処理 | 参照 |
| --- | --- | --- |
| 絞り込み（検索） | TBD | 共通仕様 §3 |
| TBD | TBD | TBD |

### 2.2 明細のボタン（各行）

| ボタン / 操作 | 実行される処理 | 参照 |
| --- | --- | --- |
| TBD | TBD | TBD |

## 3. ルーティング / レイヤ

| 項目 | 値 |
| --- | --- |
| URL | `TBD` |
| Controller | `TBD` |
| Service | `TBD` |
| Repository | `TBD` |
| Inertia ページ | `TBD` |
| Vue ページ | `TBD` |

## 4. 表示データ・抽出条件

- 対象テーブル: TBD
- 抽出条件: TBD
- 並び順: TBD

## 5. 画面レイアウト（明細テーブルの列）

| 列 | 内容 | 備考 |
| --- | --- | --- |
| TBD | TBD | TBD |

## 6. 操作仕様

### 6.1 フロー

TBD

### 6.2 入力

TBD

### 6.3 処理

TBD（金額計算を伴う場合は BCMath を使用する。→ [`backend.md`](../../architecture/backend.md) §5.5）

### 6.4 応答

- 成功: TBD
- 0件: TBD
- 例外: `ServiceException` → トースト。

## 7. felix_total 連携

- 連携の要否: TBD（不要なら「なし」と明記する）
- 呼び出す現行 API / 更新対象: TBD
- 処理順・ロールバック方針: TBD（原則「新テーブルを更新 → 現行 API」／失敗時はロールバック）

## 8. 共通機能の利用

- **やり取り（コメント）**: TBD（共通仕様 §4）
- **iframe モーダル**: TBD（共通仕様 §5）
- **フィルタ**: TBD（共通仕様 §3）
- **添付ファイル**: TBD（[99_添付ファイル_詳細設計.md](../quotations/99_添付ファイル_詳細設計.md) 参照）

## 8.5 画面の表示条件

> 出典: **見積管理_処理フロー**（H〜L 列）。

### アクション

**現時点では次にすべきアクションが無いため表示のみ。**
処理フロー上に「それなら例の『ステータス確認』画面で足りるかも？」というコメントがあり、
**独立画面として残すか、ステータス確認画面へ統合するかは未決**（→ §9）。

### 初期表示条件

- 区分が**支払**
- **選定業者を承認**（部長承認済み）

### 絞り込み条件

> 表の見方（処理フロー I列の記法）
> - **単一選択**（`・` 区切り）… セグメント／ラジオ。**初期値**の列が画面初期表示時の選択。
> - **チェックボックス**（`・` 区切りなし）… 単独のトグル。**初期値**が ON のものは初期表示時からチェック済み。

| 項目 | 形式 | 選択肢 | 初期値 |
| --- | --- | --- | --- |
| 区分 | 単一選択 | 全て / 支払 | **支払** |
| コメント | 単一選択 | 全て / コメントあり / コメントなし | **全て** |


> **区分の切り替え**
> - **支払**（初期値）… `t_payable_partners` の取引先だけを表示する。
> - **全て** … 支払に加えて `t_billing_partners`（請求）の取引先も**同じ一覧に並べる**。
>   請求行は承認ステータスに関わらず**表示のみ**で操作できない（サーバが `operable = false` を返す）。

### 表示する承認ステータス

| ステータス | 条件 |
| --- | --- |
| `APPROVED`（承認済） | かつ **発注テーブルにデータがあること** |

### 表示ステータス以外のデータの扱い

**それ以前のステータスのデータは見積管理側に表示し、本画面には出さない。**
＝ 見積管理と発注管理でデータが重複して見えないよう、ステータスで担当画面を分ける。

### 業者マイページ側の操作

なし（請負承認は部長承認後の発注書画面で行う。
→ [03_支払_部長承認](../quotations/03_支払_部長承認_詳細設計.md) §7.4）。

## 9. 留意事項

- TBD
- 「請求（もらい）」側の画面は現行 felix_total に対応機能が無い可能性がある。
  既存データ・現行画面との関係を確認したうえで仕様を確定すること。
