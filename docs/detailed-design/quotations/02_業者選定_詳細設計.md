# 業者選定 詳細設計書

> 共通の仕組み（ヘッダー / フィルタ / やり取り / iframe / テーマ / 送信・エラー方針）は
> [共通仕様_詳細設計.md](./00_共通仕様_詳細設計.md) を参照。本書は本画面固有の差分を記述する。

## 1. 概要

- **画面名**: 業者選定【業者→FELIX返答済】
- **mode**: `vendor-selection`
- **操作ロール**: 建設部、システム開発、administrator
- **目的**: 業者から相見積の回答が返ってきた見積先の中から、発注する業者を選定（採用）して確定する。
  確定すると担当承認済（`STAFF_APPROVED`）となり、部長承認へ進む。

業務フロー上の位置づけ:

```
見積依頼 ──▶【業者選定】 ──確定──▶ 部長承認 ──▶ 部長取消申請 ──▶ 部長取消承認
                 ▲
                 └─ 部長承認で否認されると、ここへ差し戻される（UNSELECTED + deny_comment）
```

## 2. 機能説明

本画面の操作は、**画面上部の「絞り込み・操作バー」のボタン**と、**明細（各行）のボタン**の
2つのボタン群から行う。以下ではボタンごとに実行される処理を分けて記載する。

前提となる一覧表示: 業者から見積回答が返ってきた未選定（`UNSELECTED`）の見積先を「実行予算 → 項目 →
見積先」の明細で一覧表示する。部長承認で否認され差し戻された見積先（`denied`）もここに現れる。（→ §4, §5）

### 2.1 絞り込み・操作バーのボタン（画面上部）

| ボタン / 操作 | 実行される処理 | 参照 |
| --- | --- | --- |
| 絞り込み（検索） | 実行予算名 / 項目名 / 見積先名でサーバ側フィルタし、一覧を再取得する。 | 共通仕様 §3 |
| 回答状態フィルタ | 全て / 見積回答あり / 見積回答なし（最新相見積履歴の有無）でサーバ側フィルタする。 | 共通仕様 §3 |
| コメント有無フィルタ | 全て / コメントあり / コメントなしでサーバ側フィルタする。 | 共通仕様 §3 |
| 明細を全て開く / 閉じる | 全実行予算カードの明細を一括で開閉する。 | §5 |
| 全て選定 / 全ての選定を解除 | 表示中の見積回答ありの行の選定を一括で ON / OFF する。 | §6.1 |
| 発注業者を確定 | 選定した見積先を発注業者として一括確定する。felix_total の採用処理を呼び出して実行する。 | §7 |

**検索項目（絞り込み検索フォーム）**: 下記3項目を入力し「絞り込み検索」ボタンで送信すると、
条件に一致する一覧をサーバから再取得する。いずれも**部分一致（LIKE）**で、空欄の項目は条件から除外する。

| 入力欄（画面ラベル） | 検索対象 | 条件 |
| --- | --- | --- |
| 物件名 | 実行予算名（`t_buildings.building_name`） | 部分一致 |
| 項目名 | 費用項目名（`t_building_cost_items.item_name`） | 部分一致 |
| 見積先 | 見積先の会社名（`company_name`） | 部分一致 |

### 2.2 明細のボタン（各行）

| ボタン / 操作 | 実行される処理 | 参照 |
| --- | --- | --- |
| 業者選定（トグル） | サーバの選定状態を初期値に、押下で選定 / 解除をトグルする。見積回答が無い行は選定不可。 | §6.1 |
| やり取り（吹き出し） | 項目単位のコメントスレッドを開き、建設部メンバー⇔部長でやり取りする。未読バッヂ・添付に対応。 | 共通仕様 §4 |
| 見積先の詳細（会社名リンク） | felix_total の見積先編集画面を iframe モーダルで開く。 | 共通仕様 §5 |

## 3. ルーティング / レイヤ

| 種別 | ルート名 | メソッド | 実装 |
| --- | --- | --- | --- |
| 一覧 | `quotation-management.vendor-selection` | GET | `VendorSelectionController::index` |
| 確定 | `quotation-management.vendor-selection.confirm` | POST | `VendorSelectionController::confirm` |
| 仮選定保存 | `quotation-management.vendor-selection.provisional` | POST | `VendorSelectionController::provisional` |

- ページ: `resources/js/pages/EstimateManagement/VendorSelection.vue`（`action-label="発注業者を確定"`）。
- Service: `VendorSelectionService`（`paginate` / `confirm` / `setProvisional`）。
- Repository: `BuildingQuotationRepository`（`forEstimateManagement(..., 'vendor-selection')` / `recordVendorSelections` / `setProvisional`）。

## 4. 表示データ・抽出条件

- 対象ステータス: `approval_status = UNSELECTED`（業者未選定）。
- 回答状態フィルタ（`answer`）が使える。回答あり＝最新相見積履歴（`is_latest`）を持つ見積先。
- 相見積額（`quotePrice`）は最新の相見積履歴（`latestHistory.amount_excluding_tax`）を表示する。
- 否認差し戻し分（`deny_comment` あり）もここに現れる（`denied=true`）。

主な行フィールド:

| フィールド | 内容 |
| --- | --- |
| `selected` | 選定済みか（`approval_status !== UNSELECTED`） |
| `quotePrice` | 相見積（最新履歴の税抜額）。回答なしは選定不可 |
| `provisional` | 仮選定（`is_drafted === 1`）。ボタンを緑表示にする |
| `denied` | 否認差し戻し（`deny_comment` あり）。ボタンを赤表示にする |

## 5. 画面レイアウト（明細テーブルの列）

| 列 | 内容 |
| --- | --- |
| 項目 | 項目名（表示中の先頭行のみ）＋ やり取り吹き出し |
| 見積先 | 会社名（iframeで詳細表示） |
| 標準単価(税抜) | `masterPrice` |
| 予算単価(税抜) | `budgetPrice` |
| 相見積(税抜) | `quotePrice`（`showQuote=true`） |
| 業者選定 | トグルボタン（`kind=toggle-button`。未選定=`選定する` / 選定中=`選定`） |

## 6. 固有の操作仕様（toggle-button）

業者選定は他画面と異なり「サーバの選定状態を初期値に、押下でトグルする」方式（選び替え）。

### 6.1 選定トグル（`toggleSelect`）

- 選定可能なのは**見積回答あり**（`quotePrice != null && quotePrice > 0`）の行のみ。回答なしは
  ボタンを無効化（`見積回答が無いため選定できません`）。
- サーバ状態（`serverSelected` = `row.selected`）を初期値に、ローカル `selectionOverride` で上書き。
- `vendorDirty`（サーバ状態から1つでも押下変更があるか）かつ選定1件以上でヘッダー確定を活性化。
- 「全て選定／全て解除」は見積回答ありの行を対象に一括トグルする。

### 6.2 ボタンの状態色（共通仕様 §6 参照）

- 赤: 否認差し戻し（`denied`）で、同一項目のどの業者もまだ選定されていない。
- 緑: 仮選定済み、または否認差し戻しが同一項目のいずれかの選定で解消された（赤→緑）。
- ゴールド: 通常。

### 6.3 仮選定の保存（`provisional`）

チェック相当の操作で `t_cost_quotations.is_drafted` を即時保存する（見積依頼画面と同じ API）。
本画面では選定トグルの状態色（緑）判定に用いる。

## 7. 確定処理（発注業者を確定）

### 7.1 入力・フロー

1. 回答ありの見積先を選定 → ヘッダー「発注業者を確定（n）」が活性化。
2. 確定 → `ConfirmVendorSelectionRequest` へ `companyIds` を POST。
3. `VendorSelectionService::confirm()` → `BuildingQuotationRepository::recordVendorSelections()`。

バリデーション（`ConfirmVendorSelectionRequest`）:
- `companyIds`: required / array / min:1
- `companyIds.*`: integer / distinct / `exists:estimate_unit_companies,id`（旧 ID で検証）

### 7.2 felix_total 連携（`recordVendorSelections`）

- 見積先を旧 ID（`unit` = 項目 `source_id` / `company` = 見積先 `source_id`）へ写像する。
処理順は **「新テーブルを更新 → 現行 felix_total の API を呼ぶ」** で、1トランザクションにまとめる
（`syncWithFelixTotal()`）。

1. 見積先を旧 ID（`unit` = 項目 `source_id` / `company` = 見積先 `source_id`）へ写像する。
   移行元（`source_id`）の無い行は除外する。
2. `approval_status = UNSELECTED` の行だけを対象に絞る（二重送信・状態不整合の防止）。
3. 新スキーマの `approval_status` を `UNSELECTED → STAFF_APPROVED` へ UPDATE する。
4. 対象行につき felix_total の採用処理 `FelixTotalQuoteRequestGateway::adoptCompany($unit, $company)`
   を呼ぶ（`update_adoption_flg` を `mode='true'` で実行）。
5. felix_total が失敗（接続不可・非 2xx）した場合は例外が送出され、**3 の UPDATE はロールバック**される
   （両方書けたときだけ成功として扱う）。

戻り値は 3 で実際に遷移した件数。

### 7.3 応答

- 成功: `back()->with('success', "発注業者を確定しました。（n件）")`。
- 0件: `back()->with('error', '発注業者を確定できませんでした。選定した見積先をご確認ください。')`。
- 例外: `ServiceException` → トースト。

## 8. 共通機能の利用

- **やり取り（コメント）**: 全画面共通。担当（staff）視点で発言（共通仕様 §4）。部長承認画面の部長と
  同じ項目スレッドを共有する。否認・取消の理由コメントもここに現れる。
- **iframe モーダル**: 「見積先の詳細」（会社名リンク）（共通仕様 §5）。
- **フィルタ**: `keyword` / `itemLabel` / `vendor` / `answer` / `comment`（共通仕様 §3）。

## 9. 留意事項

- `no_competitive_flg` は現行 `estimate_units` に列が無いため 0（相見積あり＝単一採用）で渡す。
- 送信成功後はリロードされるため、ローカルの選定上書き（`selectionOverride`）はクリアする。
